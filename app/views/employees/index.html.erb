<h1>Dunder Mifflin Employees:</h1>
<h2>
    <ul>
    <%@employees.each do |e|%>
    <li><%=link_to e.full_name, employee_path(e) %>
    <%end%>
  </ul>
  <h3> <%= button_to 'Create Employee', new_employee_path, :method => :get %></h3>
</h2>
