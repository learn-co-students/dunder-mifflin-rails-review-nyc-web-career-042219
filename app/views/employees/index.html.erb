<h1>All Employees</h1>
<%@employees.each do |employee|%>
    <h3><%= link_to employee.name, employee_path(employee)%></h3>
    <%=button_to "Delete Employee", employee_path(employee), method: :delete, data: {confirm: "Are you sure?"}%>
<% end %>